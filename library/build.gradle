apply plugin: 'com.android.library'
apply plugin: 'com.novoda.bintray-release'

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    defaultConfig {
        minSdkVersion 9
        targetSdkVersion rootProject.targetSdkVersion
        versionCode 1
        versionName "1.0.0"

        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"

    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    provided "com.android.support:appcompat-v7:$rootProject.supportLibraryVersion"

    // RxJava
    provided "io.reactivex.rxjava2:rxandroid:$rootProject.rxandroidVersion"
    provided "io.reactivex.rxjava2:rxjava:$rootProject.rxjavaVersion"

    testCompile "junit:junit:$rootProject.ext.junitVersion"

}

publish {
    userOrg = 'listenzz'
    groupId = 'com.shundaojia'
    artifactId = 'live'
    publishVersion = '1.0.0'
    desc = 'A Rx Transformer handle Android Lifecycle as same as LiveData.'
    website = 'https://github.com/listenzz/Live'
}
